# 05｜决策记录与开放问题

> 用途：区分“已经由源码证据冻结的决定”和“必须由实际部署/产品选择回答的问题”。  
> 状态词：`ACCEPTED` 可直接施工；`CONDITIONAL` 满足条件后施工；`DEFERRED` 当前不进入范围；`OPEN` 尚未裁决。

## 1. 已冻结 ADR

### ADR-001｜领域对象是整文件

- **状态**：`ACCEPTED`
- **决定**：以 `{folder, file}` 作为 `DiaryNoteKey`，正文是一个不透明字符串。
- **依据**：管理 API 的 list/get/save 粒度均为文件；结构化时间来自可选文件名解析。
- **后果**：不建立 `DiaryEntry`、entry ID、entry count 或条目级写入；未来出现可靠正文 schema 时另立 ADR。

### ADR-002｜一个全局 Diary 页面，内部切换状态

- **状态**：`ACCEPTED`
- **决定**：overlayStore 只新增 `diaryCenter`；list/reader/editor/preview 是 feature 内部状态。
- **依据**：项目主要以 Overlay/SlidePage 导航；Diary 状态共享强、路径短。
- **后果**：不新增 Router，不把 reader/editor 各注册成永久全局页面；内部返回由 ModalHistory 接管。

### ADR-003｜Rust 是远端访问与安全边界

- **状态**：`ACCEPTED`
- **决定**：URL、Basic Auth、reqwest、超时、response budget、schema、hash 与错误归一化全部留在 Rust。
- **依据**：VCPMobile 已有 SettingsState、Basic Auth 和有界网络模式；Vue 不应接触管理员密码。
- **后果**：Vue 只 invoke typed command；错误消息和日志不得包含凭据或完整远端 body。

### ADR-004｜一个 Composition Pinia Store

- **状态**：`ACCEPTED`
- **决定**：folder/list/search/document/editor 使用一个 `useDiaryStore`。
- **依据**：同一交互旅程中的 target、generation、baseline 和 dirty 状态需要原子协调。
- **后果**：不建立 shelf/reader/editor 平行 Store，不复制 overlayStore 或 ModalHistory。

### ADR-005｜管理 API 与 Human Tool 分离

- **状态**：`ACCEPTED`
- **决定**：浏览/搜索/整文件保存使用 admin Basic；DailyNote/LightMemo 使用 Bearer API Key。
- **依据**：VCPChat 真实调用链和官方接口是两套鉴权。
- **后果**：配置态、错误态、可用性探测与 UI 文案分开；不能用“API Key 已配置”代表管理能力可用。

### ADR-006｜所有响应必须有 Mobile 预算

- **状态**：`ACCEPTED`
- **决定**：采用流式累计、checked-add、解码后数量/长度二次校验；超限 fail closed。
- **依据**：服务端单文件 GET 当前没有大小上限，移动端不可直接无限读入。
- **后果**：不静默截断后允许编辑；具体字节数等待 P0 数据校准。

### ADR-007｜所有读请求有 generation 与 target 提交门

- **状态**：`ACCEPTED`
- **决定**：folders/list/search/document 独立 generation；success、error、finally 都核对 owner 和完整 target。
- **依据**：桌面 list/read 没有 owner，存在迟到响应覆盖；VCPMobile 已有同类并发治理模式。
- **后果**：刷新保留旧内容，旧请求只可丢弃；搜索还在 Rust 侧取消旧 owner。

### ADR-008｜保存采用 best-effort 冲突预检

- **状态**：`CONDITIONAL`
- **决定**：保存前 GET + baseline hash，匹配后 POST，再 GET read-back；force 必须由用户二次确认。
- **条件**：P0 证明 raw-save 能正确刷新相关索引；若服务端提供原子条件写则优先升级。
- **后果**：明确承认 GET→POST 的 TOCTOU；不能称为 CAS，超时不能自动重试。

### ADR-009｜远端日记按不可信 HTML 处理

- **状态**：`ACCEPTED`
- **决定**：feature-local `marked → strict DOMPurify`；禁止 raw active content，外部图片默认不自动跨 origin 加载。
- **依据**：桌面直写 `innerHTML` 不安全；现有 `astRenderer` 明确不是通用 sanitizer。
- **后果**：不直接复用桌面渲染或 HtmlPreviewBlock 放宽配置；文件名、摘要、错误、高亮均使用文本节点。

### ADR-010｜首版不做离线数据库或写队列

- **状态**：`ACCEPTED`
- **决定**：VCP 服务是唯一正文真相；只在内存保留当前正文、baseline 和草稿。
- **依据**：首版没有多端合并协议、revision 或离线冲突模型。
- **后果**：无网时已加载正文可读，但不排队 save/delete/move；Android 杀进程后的草稿恢复不作承诺。

### ADR-011｜不预先引入列表窗口化与重型渲染器

- **状态**：`ACCEPTED`
- **决定**：先以普通线性列表和轻量 Markdown 完成真机 profile。
- **依据**：实际目录规模未知；项目已具备 `useVirtualList`，需要时可复用，无需新增依赖。
- **后果**：只有稳定复现的渲染瓶颈才启用窗口化；KaTeX/Mermaid/高亮按需加载并单独验收。

### ADR-012｜移动表达是高密度线性工具，不是桌面玻璃工作台

- **状态**：`ACCEPTED`
- **决定**：实色表面、细分隔、2px Accent Bar、连续滚动、显式编辑。
- **依据**：项目 UI 宪法与手机信息密度；桌面组件没有响应式布局且重度 blur/Canvas。
- **后果**：内容区无 backdrop blur、大卡片、厚阴影、hover-scale、人为等待或持续力导图动画。

### ADR-013｜首版不提供 rename 假能力

- **状态**：`ACCEPTED`
- **决定**：标题/文件名在阅读和编辑页只读。
- **依据**：桌面标题输入不会改变保存 target，管理 API 未确认独立 rename。
- **后果**：未来若以 move/rename 实现，必须设计重名、部分失败和 baseline 迁移契约。

### ADR-014｜首个发布范围到普通搜索与受保护编辑为止

- **状态**：`ACCEPTED`
- **决定**：P1 只读浏览，P2 普通搜索 + 编辑构成 Release 1；管理、语义、工作台、关系能力后置。
- **依据**：这覆盖“随时查看、编辑 Agent 记忆”的核心目标，同时控制首版风险面。
- **后果**：不能因为桌面已有图谱/工作台就扩大首版；后续能力按实际任务收益立项。

### ADR-015｜语义层名称服从当前源码

- **状态**：`ACCEPTED`
- **决定**：Diary 只使用 `z-page`、`z-sheet`、`z-dialog` 等语义名，不复制数值。
- **依据**：当前源码、Uno 配置、主题和 tracked docs 一致为 `editor=70, viewer=80`，与所附指令表存在漂移。
- **后果**：实现评审以 current source 为准；不为 Diary 修改全局层级数字。

## 2. P0 开放问题

| ID | 问题 | 所需证据 | 保守默认 | 是否阻断 |
|---|---|---|---|---|
| Q-001 | 实际部署对应哪个 VCPToolBox 版本/分支？ | 服务端版本或部署 SHA；端点 fixture | 只依赖消费端证实的最小字段 | 阻断契约冻结 |
| Q-002 | `vcpServerUrl` 是否带反代 path prefix？ | 脱敏配置样本和请求成功 URL | 保留配置 path，禁止只截取 origin | 阻断 URL 实现定型 |
| Q-003 | admin 路由是否始终为 `/admin_api/dailynotes`？ | 实服探测、反向代理配置 | 作为已知后缀拼到规范化 base | 阻断联调，不阻断纯逻辑 |
| Q-004 | folders/list/search 的异常响应长什么样？ | 401/403/404/429/5xx 与畸形响应 fixture | 映射稳定 error code，不展示原 body | 阻断错误验收 |
| Q-005 | 实服是否有 ETag/revision/If-Match？ | 响应 header 和条件写探测 | 无原子条件写，使用 hash 预检并明确 TOCTOU | 阻断保存最终协议 |
| Q-006 | admin raw-save 是否刷新 KnowledgeBase/RAG 索引？ | 保存前后 read + search/RAG 可见性与延迟 | 未证明则 Release 1 保持只读 | **阻断写入发布** |
| Q-007 | 真实正文和列表规模是多少？ | folder/file/body P50/P95/max | 使用 03 的临时预算并 fail closed | 阻断预算定型 |
| Q-008 | `lastModified` 的格式与时区是否稳定？ | 多部署样本 | 解析失败显示原文本，不参与 key/排序 | 不阻断 |
| Q-009 | `MusicDiary` 应隐藏还是可选显示？ | 产品决定 + 真实数据用途 | 不静默过滤，必要时放入普通文件夹列表 | 阻断最终 IA 小项 |
| Q-010 | “日记/知识库/思维簇”命名规则是否长期稳定？ | 文件夹样本和用户心智验证 | 仅展示分组，不产生领域权限/行为 | 不阻断 |
| Q-011 | 是否存在 Agent ID → folder 的稳定映射？ | 服务端 schema 或配置契约 | 不从 Agent 卡片自动猜目录 | 阻断深链，不阻断中心页 |
| Q-012 | Markdown 中图片/附件 URL 的真实形态？ | 脱敏正文样本、资源认证与 origin | 跨 origin 图片不自动加载 | 阻断附件体验，不阻断纯文本 |
| Q-013 | 超大/非 UTF-8/空文件如何分布？ | 边界 fixture | 超限/非法 UTF-8 局部报错；空文档可读可编辑 | 阻断边界验收 |
| Q-014 | POST 超时后的服务端行为是否可复现？ | 故障注入 + read-back | 标记 uncertain，绝不自动重写 | 阻断保存故障验收 |
| Q-015 | tool create/update 的字符串转义是否可靠？ | 引号、换行、反斜杠、emoji 样本 | P3 前不开放创建 | 不阻断 Release 1 |

## 3. 产品待决项

这些问题不应由工程实现偷偷代答：

### 3.1 冲突时是否允许强制覆盖

推荐默认：允许，但只在展示“远端已变化”的冲突层中提供二级动作，并再次确认。主按钮应是“保留草稿并返回比较”，不是覆盖。

如果产品选择完全禁止 force，则保留草稿并允许复制/分享，等待服务端提供 merge/rename 能力。

### 3.2 阅读页是否需要字号偏好

推荐 Release 1 先遵循系统字体缩放并采用 16px、1.65 的稳定基线。只有用户测试证明系统缩放不足时，再加 feature-local 字号偏好；它不进入 Delta Sync。

### 3.3 搜索默认范围

推荐默认当前文件夹，范围标签始终可见；用户切换到“全部文件夹”后只在本次 Diary 会话内记忆。这样避免每个字符都触发更重的全局搜索。

### 3.4 外链和外部图片

推荐：链接明确确认后交给受控 opener；跨 origin 图片默认显示占位和“加载图片”动作，不静默泄露 IP、Referer 或资源 token。最终策略依 Q-012。

### 3.5 草稿恢复承诺

推荐 Release 1 只承诺页面生命周期内保留。若要承诺进程被杀后的恢复，需要单独 ADR：加密/过期/容量/多草稿 key/远端基线失效都必须同时解决，不能顺手塞入 localStorage。

## 4. P0 证据包规范

建议未来在测试目录中维护脱敏、最小、可复现的契约证据，而不是把生产响应粘进计划文档。

候选结构：

~~~text
src-tauri/tests/fixtures/diary/
├─ folders.success.json
├─ folder.success.json
├─ note.empty.json
├─ note.unicode.json
├─ search.success.json
├─ search.limited.json
├─ error.unauthorized.json
├─ error.malformed.json
└─ README.md
~~~

规则：

- 文件名、正文、folder 全部替换为人工构造值；
- 不保存 Authorization、cookie、真实 URL、用户名、密码、Agent 私密内容；
- 记录状态码、相关 header、内容类型、schema 和 byte size；
- 记录采集日期与服务端版本，但不记录内网地址；
- fixture 使用相对路径，通过 `include_str!`/`include_bytes!` 加载；
- 任何真实正文只用于本地人工验证，不进入 Git。

## 5. P0 退出评审

P0 评审至少回答：

- [ ] Q-001 至 Q-007 均有证据或书面保守默认；
- [ ] Q-006 已证明，或产品接受 Release 1 暂时只读；
- [ ] URL/redirect/Basic Auth 边界已冻结；
- [ ] DTO 只含实服稳定字段；
- [ ] response budget 基于实测分布并留有明确余量；
- [ ] 保存协议准确命名为 CAS、条件写或 best-effort 预检之一；
- [ ] Markdown 附件策略不会让主 DOM 接收活动内容；
- [ ] `MusicDiary` 与分类规则没有静默丢数据；
- [ ] fixture 全部脱敏并通过隐私复查；
- [ ] 04 的 Release 1 验收项能被自动测试或真机步骤验证。

评审结果记录为：

~~~text
P0 status: PASS | PASS-READ-ONLY | BLOCKED
server evidence date:
server version/ref:
write/index verdict:
size budget verdict:
remaining accepted risks:
reviewers:
~~~

## 6. 变更控制

以下变化必须更新 01、03、04、05 中至少对应一份，而不能只改实现：

- 服务端新增 revision/ETag/条件写；
- list/search DTO 新增稳定 ID、size、tags 或 match offset；
- 正文获得正式条目 schema；
- 管理 API 改鉴权或 base path；
- raw-save 与索引的协调语义变化；
- 产品加入离线、跨设备偏好、rename 或多文档工作台；
- VCPMobile 的 overlay/layer/settings 所有权发生变化。

文档的责任是标明“事实变化导致哪个决定失效”，而不是永久冻结今天的实现细节。
