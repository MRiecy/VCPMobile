# 07 · 施工验收记录（2026-08）

> S0 / S1 / S2a / S2b 全部完成并通过静态门。本文档归档交付物与偏差。

## 交付总览

| 阶段 | 内容 | 提交 |
| --- | --- | --- |
| 基线 | 计划文档集 01–06 | `bf819bd` |
| S0 | HTTP 连接画像注册表（`http_clients.rs`：ChatStream/AdminApi），聊天热路径等 4 处迁移 | `d89884a` |
| S1 Rust | `infra/admin_api.rs` 共享助手 + `logcenter`（fetch/clear） | `2225e68` |
| S1 前端 | 日志中心页面 + 「更多」入口 + 23 个测试 | `de5cb47` |
| S2a Rust | `taskcenter` 五条 command（含全局开关 read-modify-write） | `d0eb8a1` |
| S2a 前端 | 任务调度只读批（列表/启停/触发/历史/全局开关） | `ffc7a37` |
| S2b Rust | 任务 CRUD + Agent 列表投影 + 委托端点 | `4129d38` |
| S2b 前端 | 任务编辑器 + 异步委托追踪 | `5f60276` |
| 文档 | vue_docs 两册 | `a18d67d` / `999a461` |

## 静态门

- `pnpm check`（vue-tsc + cargo check --locked）：零错误零警告；
- 前端 Vitest：**370 通过**（新增日志中心 23 + 任务中心 24）；
- Rust `cargo test --locked --lib`：**349 通过**（新增 14）。

## 与方案的偏差记录

1. **Download 画像未落地**：updater 按裁决不迁移后 Download 无消费者，
   为避免推测性 API 已移除；将来出现第二个下载类消费者再加（http_clients.rs 内有注释）。
2. **`pollOnce` 公开化**：两个 Store 的单次轮询 tick 提为公开动作，供测试直接驱动。
3. **测试 mock 陷阱记录**：验证乐观更新中间态时，invoke mock 必须返回
   `Promise.reject` 而非同步 throw（同步 throw 会在 await 挂起前完成回滚）。
   已写入任务中心文档。
4. **委托详情端点未用**：按决策 5（展示+取消足够），
   `GET /delegations/:id` 未接入；`normalizeDelegations` 兼容数组与包裹两种响应形态
   （上游响应形态未逐字段验证，做了防御归一化）。

## 待真机验收项（模拟器/真机手动）

- [ ] 日志中心：5000 行滚动帧率；退后台 5 分钟回前台增量无断行；清空服务器日志链路；
- [ ] 「更多」popover 三列六按钮的触控高度（≥44px）与窄屏排版；
- [ ] 任务中心：创建 custom_prompt 任务 → 手动触发 → 历史出现记录全链路；
- [ ] 任务编辑器软键盘下的占位符 chips 插入与 datetime-local 原生控件体验；
- [ ] 委托取消确认链路。

## 后续 backlog（未排期）

- 03 篇插件中心 10 条灵感（用户裁决：本期不动）；
- 日志归档浏览（需后端新 API）；筛选多关键词/排除语法；长按单行复制；
- 任务全局开关的状态推送（后端 `broadcastStatusUpdate` 目前是日志占位）。
