<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <queries>
        <package android:name="com.topjohnwu.magisk" />
        <package android:name="me.weishu.kernelsu" />
        <package android:name="me.tool.apatch" />
    </queries>

    <application>
        <service
            android:name=".service.StreamKeepaliveService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="remoteMessaging"
            android:stopWithTask="true" />

        <!-- 隔离进程推送接收服务：仅加载极简 Kotlin 与网络监听，占用约 15MB 内存，完美对抗 LMK 后台清理 -->
        <service
            android:name=".service.PushListenerService"
            android:process=":push"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="remoteMessaging" />

        <!-- 隔离进程通知栏交互响应接收器：同样运行在 :push 进程，用于在后台直接响应【允许】/【拒绝】并进行异步 POST 请求 -->
        <receiver
            android:name=".service.PushActionReceiver"
            android:process=":push"
            android:enabled="true"
            android:exported="false" />
    </application>
</manifest>
