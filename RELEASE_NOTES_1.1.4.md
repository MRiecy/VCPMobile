# VCP Mobile v1.1.4 发布说明

> 版本号：v1.1.4 · versionCode 1001004 · 发布：2026-08 · 平台：Android 8.0+（minSdk 26）· 架构：arm64-v8a
>
> 距 v1.1.3（Guardian Protocol）共 88 个提交，聚焦四大主线：**全新内置 CLI 运行时（VCPMobileCLI）**、**交互教学指引（Coachmark Guide Tour）**、**日记中心**、**同步 Wire 1.2 错误契约**，以及移动端性能与 UI 细节的全面打磨。

---

## ✨ 新功能

### VCP Mobile CLI（全新内置工具运行时）

- **本地 CLI 运行时**：前台运行、任务后台续跑（best-effort job survival）、作业查询/取消（`run | poll | cancel | list`）。
- **真实 PTY 终端**：Android 原生 PTY 辅助库（`libvcp_pty.so`）+ PRoot 环境，内置 Alpine 3.24.1 aarch64 rootfs，支持交互式命令会话。
- **Skill 生命周期**：受管 Skill 目录（提示词技能记忆）、离线语义召回（semantic river recall）、本地 vref 知识契约。
- **分布式适配**：可选分布式适配器，超时请求支持 `cancel_tool` 能力；通信超时提升至 30s。
- **资源边界放宽**：PRoot 子进程内存上限 512 MiB → 4 GiB，大型脚本不再被 OOM 中断。
- **前端工作台**：工具说明（manifest 面板）、Jobs/Skills/终端三页签工作台，Manifest 契约与 Rust 侧逐字节对齐。
- **运行时收口**：Job 状态机新增 `Stopping` 中间态（取消意图首次落盘即置 `Stopping` 且永不回退 `Running`，进程退出事实确认后才宣称终态，重启后 reconcile 语义不变）；`list` 按调用方会话隔离可见性（仅见本会话与无主作业）；子进程环境注入 `VCP_CLI_WORKSPACE` 与按需 `VCP_CLI_SESSION_ID`；Kotlin 宿主进程退化为哑执行器，租约丢失后的收容统一由 Rust 监视器经取消通道收口。

### 交互教学指引（Coachmark Guide Tour）

- **自研 Coachmark 引擎**：`GuideOverlay` 覆盖层纳入语义化层级体系（新增 `z-guide=95`，层级体系由 12 级扩展为 13 级）；注册表 + `guideStore` 完成态持久化与版本门控（`introducedIn`），为未来新功能上线时的版本化指引预留能力，全程零新增依赖。
- **首期四场指引**：左边栏手势（右滑设置 + 按住拖动排序）、长按亮暗模式按钮（消息呈现菜单）、聊天框 + 号（单击附件 / 长按上下文注入规则）、长按日记项（多选批量移动 / 删除）。
- **真实业务联动**：教学期间手势仅演示；用户点击“下一步”离开步骤时执行真实效果（步骤级 `perform` / `undo` 钩子 + 整场逆序清理），教学节奏完全由用户掌控，无自跑定时器。
- **防误触发**：前置条件模型（工作区被页面栈 / 抽屉遮挡时不播）、严格 FIFO 队列、日记中心首开等待放宽，避免指引在遮挡层后“幽灵播放”。
- **聚光动画**：veil / frame 双层聚光（入场自四周聚焦收敛，步骤间平滑滑移），手势演示使用构建期内联图标。
- **回放与重置**：设置 → 帮助与指引新增“交互教学指引”子页，列出全部指引的标题 / 说明 / 完成状态，支持单项重播与“重置全部进度”入口。

### 日记中心

- 远端日记服务 + 移动端日记中心（列表、搜索、文件夹视图）上线。
- 暗色主题下日记样式与侧栏行为修复。

### 主题与呈现

- 主题预览（ThemeLivePreview）与消息呈现模式（presentation modes）上线。
- 可展开侧栏工具托盘，常用工具随取随用。
- 全局确认对话框：危险操作统一确认与错误处理规范。

### 同步

- **Wire 1.2 错误契约**：统一错误码体系与排障规范，前端固定展示历史错误而非原始命令失败。
- 协议 1.2 移动端持久化与传输收口；topic owner 身份精确携带。

---

## 🔧 修复与优化

- **全局设置首开提速**：设置页 6 个重量级区块（头像裁剪、模型选择器、主题选择器、同步设置等）改为按需懒加载；冷启动首次打开模块数 33 → 9，内容可见时间约 +430ms（含冷编译），不再拖拽整页首屏。
- **CLI 工具说明页修复**：Jobs / Skills 标签下点击“工具说明”此前不显示内容，现已正确展示 manifest 面板。
- **UI 细节统一**：日记中心与 CLI 工作台关闭按钮由 X 统一为 ‹；侧栏“日记中心 / 更多”按钮阴影减淡，与相邻按钮视觉一致；消息呈现模式（panel / immersive）内边距微调。
- **触觉反馈**：智能体列表拖动排序开始时增加 20ms 轻触觉（不支持震动的设备静默降级）。
- **WebView 性能**：消除常驻刷新与首屏冗余渲染。
- **设备兼容**：Android 多设备兼容与规范、权限检查/自启动/省电策略提示优化。
- **安全边界**：富 HTML 主动内容边界收敛；前端与 Tauri 供应链更新。
- **格式兼容**：DailyNote 新格式兼容。

---

## 🛠 工程与质量

- **Agent 安全调试工具链**：新增低噪声 USB/HMR Debug Agent（`pnpm android:debug:*`），PID 隔离日志、有限快照，不触碰正式安装包。
- **构建供应链加固**：Gradle 依赖严格校验（verification-metadata.xml）、CI 修复、`tauri.settings.gradle` 停止跟踪。
- **测试规模**：Rust 内联单测 323 通过；Vitest 前端测试 279 通过（45 文件，其中指引引擎专项 48 项，覆盖状态机、队列、锚点契约、触发接线与指令）；`vue-tsc` + `cargo check` + 生产构建全绿。
- **文档**：WebView 性能兼容研究、日记架构、同步 Wire 1.2 契约、CLI 收敛 ADR 与真机验收存档，以及指引系统专项讨论档案（Coachmark 方案、教学点证据、触发边界与三方审查）。

---

## 📦 安装与升级

| 项目 | 说明 |
| --- | --- |
| APK | `VCPMobile_v1.1.4_arm64-v8a.apk`（由发布流水线自动构建并挂载至本 Release） |
| 校验 | 同名 `.sha256` 文件，下载后可用 `sha256sum -c` 校验完整性 |
| 系统要求 | Android 8.0+（minSdk 26），仅支持 arm64-v8a 设备 |
| 升级方式 | 与 v1.1.3 同签名，直接覆盖安装即可，数据无损 |
| 首次安装 | 需在系统设置中允许“安装未知应用” |

发布构建由 GitHub Actions 自动执行：APK 经 apksigner 验签并拒绝调试证书，仅产出签名 arm64 APK 及其 SHA-256 文件。
