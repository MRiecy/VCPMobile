---
title: 总览与导航
scope: 双端
related_files:
  - docs/sync/*.md
---

# 总览与导航

## 1. 知识库概览

本文档集描述 VCPMobile 与 VCPChat 的当前双向增量同步实现。代码与共享 golden fixture 是最终协议事实。

### 适用读者

| 读者类型 | 阅读目标 |
|---------|---------|
| **Rust 开发者** | 理解移动端 Pipeline、Executor、Manifest、Hash 体系的实现细节 |
| **Node.js 插件开发者** | 理解桌面端索引、Diff 引擎、传输层路由与实体生命周期 |
| **维护者** | 掌握端到端数据流、冲突仲裁策略和排障边界 |

---

## 2. 文档地图

### 2.1 架构与原理（01–03）

| 文件 | 标题 | 内容 | 适用场景 |
|------|------|------|---------|
| `01_架构总览与设计理念.md` | 架构总览与设计理念 | 双端分层、三阶段协议、模块职责 | 第一次接触同步系统 |
| `02_数据模型与类型系统.md` | 数据模型与类型系统 | DTO 白名单、字段映射、数据库模型 | 查询字段契约 |
| `03_哈希体系与变更检测.md` | 哈希体系与变更检测 | 三级哈希、聚合规则、哈希冒泡 | 排查 Hash 不一致 |

### 2.2 协议与流程（04–06）

| 文件 | 标题 | 内容 | 适用场景 |
|------|------|------|---------|
| `04_同步协议详解.md` | 同步协议详解 | WebSocket + HTTP、EntityState、LWW、版本握手 | 理解完整协议交互 |
| `05_流水线与阶段控制.md` | 流水线与阶段控制 | PipelinePhase、Phase 1→2→3、命令通道 | 调试阶段卡住 |
| `06_清单构建与比对.md` | 清单构建与比对 | Manifest 与实体 Diff 算法 | 理解先比对后传输 |

### 2.3 实现细节（08–10）

| 文件 | 标题 | 内容 | 适用场景 |
|------|------|------|---------|
| `08_移动端执行器详解.md` | 移动端执行器详解 | Pull / Push / Delete 与批量传输 | 追踪 HTTP 和落库链 |
| `09_桌面端插件架构.md` | 桌面端插件架构 | 生命周期、路由、监听器和索引 | 理解桌面端服务 |
| `10_实体与消息传输.md` | 实体与消息传输 | 实体 REST、消息 NDJSON、附件 | 查询物理传输链 |

### 2.4 质量保障（11–13）

| 文件 | 标题 | 内容 | 适用场景 |
|------|------|------|---------|
| `11_冲突解决与边界情况.md` | 冲突解决与边界情况 | LWW、Hash 平局、墓碑 | 排查覆盖与删除 |
| `12_性能优化与资源管理.md` | 性能优化与资源管理 | 并发门、写队列、尺寸预算 | 排查性能与内存 |
| `13_日志系统与可观测性.md` | 日志系统与可观测性 | Session→Phase→Operation 日志 | 根据日志定位故障 |

### 2.5 规范与参考（16 + 附录 A–C）

| 文件 | 标题 | 内容 | 适用场景 |
|------|------|------|---------|
| `16_Wire_1.2错误契约与排障规范.md` | Wire 1.2 错误契约与排障规范 | 错误对象、传播与展示 | 排查错误边界 |
| `附录A_WebSocket消息类型完整参考.md` | WebSocket 消息参考 | 当前 WS 消息字段与处理位置 | 查询 WS 信令 |
| `附录B_HTTP端点完整参考.md` | HTTP 端点参考 | REST 请求、响应和调用链 | 查询 HTTP 接口 |
| `附录C_数据库Schema对照表.md` | 数据库 Schema 对照 | 双端表结构与映射 | 查询存储布局 |

---

## 3. 快速决策树

> 以下决策链按"我想… → 读…"的格式组织，帮助读者在 10 秒内定位目标文档。

| # | 问题/意图 | 推荐阅读 |
|---|----------|---------|
| 1 | 我想了解同步整体架构 | → 读 `01` |
| 2 | 我想知道某个字段在双端怎么映射 | → 读 `02` 或附录 C |
| 3 | 同步报 hash 不一致 | → 读 `03` + `11` |
| 4 | 我需要修改同步协议 | → 读 `02`、`04`、`16` 和共享 golden fixture |
| 5 | 同步很慢怎么优化 | → 读 `12` |
| 6 | 调试同步问题 | → 读 `13` |
| 7 | 需要查 WS 消息格式 | → 读 `04` 或 `附录A` |
| 8 | 需要查 HTTP 端点 | → 读 `04` 或 `附录B` |
| 9 | 需要查数据库表结构 | → 读 `02` 或 `附录C` |
| 10 | 某个 Phase 卡住或状态异常 | → 读 `05` |
| 11 | 消息同步范围不对（多了或少了） | → 读 `04` + `06` + `11` |
| 12 | 数据冲突或双向覆盖 | → 读 `11` |
| 13 | 桌面端插件如何启动和监听文件 | → 读 `09` |
| 14 | 移动端如何拉取/推送数据 | → 读 `08` |
| 15 | 为什么实体和消息走不同传输通道 | → 读 `10` |
| 16 | 首次安装同步的完整流程 | → 读 `01` + `04` |
| 17 | 需要 Rust ↔ Node.js 入口对照 | → 读 `01` 的模块职责矩阵 |
| 18 | 同步日志怎么看懂 | → 读 `13` |
| 19 | 软删除没有同步过去 | → 读 `11` + `08` |
| 20 | 批量下载消息时 OOM | → 读 `12` + `10` |
| 21 | 同一故障在双端显示成不同错误，或需要新增错误码 | → 读 `16` + `13` |

---

## 4. 阅读路径推荐

### 4.1 新手上路路径（第一次接触同步模块）

**目标**：建立端到端概念模型，不求细节，只求全貌。

```
步骤1: 01_架构总览与设计理念.md（理解双轨三层 + 三阶段协议）
    ↓
步骤2: 04_同步协议详解.md（理解 WS + HTTP 双通道交互）
    ↓
步骤3: 05_流水线与阶段控制.md（理解移动端内部状态机）
    ↓
步骤4: 11_冲突解决与边界情况.md（核对仲裁与删除边界）
```

### 4.2 问题排查路径（遇到具体问题时）

**目标**：快速定位根因，最小化无关阅读。

```
A. 同步失败/断开连接
   → 13（看日志）→ 05（看 Phase 状态）→ 11（看冲突规则）

B. 数据不一致（某条消息/Agent 双端不同）
   → 03（检查哈希冒泡）→ 06（检查 Manifest 比对）
   → 11（检查冲突仲裁）→ 13（对照日志时间戳）

C. 同步性能差（慢或卡顿）
   → 12（性能优化策略）→ 08（检查批量尺寸与并发）→ 10（检查传输模式）

D. Wire 错误或双端错误分类不一致
   → 16（错误契约）→ 13（日志）→ 04（传输边界）
```

---

### 4.3 协议修改检查路径

**目标**：保证修改在双端一致、不破坏现有协议。

```
1. `02`：核对 DTO 与存储字段。
2. `03`：核对 Hash 输入和冒泡路径。
3. `04`、附录 A/B：核对 Wire 与 HTTP 契约。
4. `05`、`08`、`09`：核对双端执行入口。
5. `11`、`16`：核对冲突和错误边界。
6. 更新共享 golden fixture，并运行对应契约检查。
```

---

## 5. 术语速查表

| 术语（中文） | 英文 | 说明 |
|-------------|------|------|
| 双轨三层 | Double-Track 3-Tier | 移动端内部 Rust/IPC/Vue 三层 + 双端 WS/HTTP 双通道隔离 |
| 三阶段同步协议 | Three-Phase Sync Protocol | Phase 1 Owner Metadata → Phase 2 Topic Metadata → Phase 3 Messages |
| 清单 | Manifest | 双端交换的数据指纹集合，用于"先比对后传输" |
| 差异计算 | Diff | 比对双端 Manifest 后生成的 Pull/Push/Delete 动作列表 |
| 哈希体系 | Hash Hierarchy | L1 config_hash → L2 content_hash → L3 aggregated_hash |
| Merkle Root | Merkle Root | 子实体哈希集合排序后连接再哈希的聚合指纹 |
| 哈希冒泡 | Hash Bubble Up | 子实体变更后向上层重新计算聚合哈希的级联过程 |
| 最后写入胜出 | LWW (Last-Write-Wins) | 以绝对时间戳 `ts` 为仲裁依据的冲突解决策略 |
| 软删除 | Soft Delete | 以 `deleted_at` 时间戳标记删除，物理保留数据 |
| 双哈希 | Dual-Hash | V2 协议中 config_hash 与 content_hash 分离设计 |
| 流水线 | Pipeline | 将同步流程分解为可管理、可观测的 Phase 序列 |
| 执行器 | Executor | PullExecutor / PushExecutor / DeleteExecutor 的统称 |
| 数据传输对象 | DTO | 跨端同步的最小字段子集，排除本地 UI 状态 |
| 换行分隔 JSON | NDJSON | 每行一个独立 JSON 对象，用于消息流式传输 |
| 幂等键 | Idempotency Key | 确保同一操作只执行一次的令牌 |
| 写意图锁 | Write Intent Lock | 防止文件监听器与同步写入冲突的标记机制 |
| 数据库写入队列 | DbWriteQueue | 移动端 SQLite 串行化写入与事务合并机制 |
| 信号量 | Semaphore | `NetworkAwareSemaphore`，控制同步并发数以避免 OOM |
| 会话 | Session | 一次完整的 WS 连接生命周期（握手到断开） |
| 阶段 | Phase | 会话内的逻辑分段：owner_metadata / topic_metadata / messages |
| 操作 | Operation | Phase 内的最小原子单元（单条实体处理） |
| 索引数据库 | Index DB | 桌面端 `sync_state.db`，用于快速 Diff 与哈希比对 |
| 全量重置 | Full Reset | 当哈希永久分叉时，清空移动端数据库重新全量同步 |
| 确定性哈希 | Deterministic Hash | 稳定 JSON 序列化后计算的 SHA-256，消除实现差异 |
| 粗到精 | Coarse-to-Fine | Phase 2.5 话题级排除 → Phase 3 逐消息比对的策略 |

---

> **维护提示**：新增文档后请同步调整本章节的文档地图与决策树。当前仓库未启用 `plans/`/`memory:refresh` 框架，以 tracked 文档、代码和测试为准。
