---
id: PLUGIN-LIFECYCLE-005
title: 生命周期桥接
description: 通过 ProcessLifecycleOwner 将进程级生命周期事件通过 evaluateJavascript 注入前端。v1.1.2 升级
version: 1.1.2
date: 2026-06-24
related_files:
  - src-tauri/plugins/vcp-mobile/android/src/main/java/com/vcp/mobile/LifecycleBridge.kt
  - src-tauri/plugins/vcp-mobile/android/src/main/java/com/vcp/mobile/VcpMobilePlugin.kt
  - src-tauri/plugins/vcp-mobile/android/build.gradle.kts
  - src-tauri/src/vcp_modules/infra/vcp_log_service.rs
---

# 生命周期桥接

## 1. 功能概述

通过 Android `DefaultLifecycleObserver` 监听**进程级**生命周期变化（resume / pause / stop），将事件通过 `evaluateJavascript` 直接注入前端 `window.CustomEvent`，使 Vue 层能响应应用前后台切换。

> **v1.1.2 升级**：生命周期观察从 `activity.lifecycle`（单 Activity 级）升级至 `ProcessLifecycleOwner.get().lifecycle`（进程级）。进程级观察免疫于 Activity 重建（如配置变更、多 Activity 场景），且提供完美的前后台消抖——`onResume` 仅在首个 Activity 可见时触发一次，`onPause` 仅在最后一个 Activity 隐藏时触发一次。

> **设计决策**：与 `KeyboardInsetsManager` 保持一致，使用 `evaluateJavascript` + `window.addEventListener` 通道，前端无需引入 Tauri 事件 API 即可接收。

---

## 2. 代码结构

```
src-tauri/plugins/vcp-mobile/android/.../LifecycleBridge.kt (~92 lines)
├── LifecycleBridge : DefaultLifecycleObserver
│   ├── attach(activity: Activity, webView: WebView)
│   │     └── activity.runOnUiThread {
│   │           ProcessLifecycleOwner.get().lifecycle.addObserver(this)
│   │         }
│   ├── onResume(owner: LifecycleOwner)   ← 进程进入前台
│   ├── onPause(owner: LifecycleOwner)    ← 进程进入后台
│   ├── onDestroy(owner: LifecycleOwner)  ← 清理观察者
│   ├── emit(eventName, detail)
│   ├── serializeValue(value): String
│   └── escapeJson(s): String
```

### 2.1 新增依赖

```kotlin
// build.gradle.kts
implementation("androidx.lifecycle:lifecycle-process:2.7.0")
```

`lifecycle-process` 库提供 `ProcessLifecycleOwner`，追踪整个应用进程的生命周期（而非单个 Activity）。当首个 Activity 恢复时进程进入前台，最后一个 Activity 暂停时进程进入后台。

### 2.2 与旧版的关键差异

| 维度 | v1.1.1 | v1.1.2 |
|------|--------|--------|
| 观察对象 | `activity.lifecycle` | `ProcessLifecycleOwner.get().lifecycle` |
| 回调接收 | 每个 Activity 独立触发 | 进程级统一触发 |
| 配置变更 | 可能导致重复回调 | 免疫 Activity 重建 |
| 事件类型 | resume/pause/stop/config-changed/low-memory | resume/pause（精简） |

---

## 3. 事件类型与触发时机

| 事件名 | 触发时机 | Payload | 前端典型用途 |
|--------|----------|---------|-------------|
| `vcp-lifecycle` | `onResume` | `{ "state": "resume" }` | 恢复 WebSocket 连接、刷新未读消息、调用 `set_app_foreground_state(true)` |
| `vcp-lifecycle` | `onPause` | `{ "state": "pause" }` | 暂停动画、保存草稿、调用 `set_app_foreground_state(false)` |

> **v1.1.2 精简**：移除了 `onStop`、`onConfigurationChanged`、`onLowMemory` 三个事件。进程级生命周期不区分 pause/stop；深色模式由前端 CSS 媒体查询检测；内存告警改用更细粒度的平台 API。

## 4. 前端与 Rust 联动（v1.1.2 新增）

生命周期事件不仅注入前端，还桥接至 Rust 侧：

```typescript
// App.vue — onPause 事件处理
window.addEventListener('vcp-lifecycle', async (e: CustomEvent) => {
    const { state } = e.detail;
    if (state === 'pause') {
        // 通知 Rust 后端"应用进入后台"
        await invoke('set_app_foreground_state', { isForeground: false });
        // → vcp_log_service: 启动 10min/5min 后台 linger 定时器
    } else if (state === 'resume') {
        await invoke('set_app_foreground_state', { isForeground: true });
        // → vcp_log_service: 取消 linger 定时器，若已断开则自动重连
    }
});
```

**Rust 侧响应**：
- `set_app_foreground_state(false)`：设置 `APP_IN_FOREGROUND = false` → 所有 log/info 事件停止注入 WebView → 启动 10 分钟 linger 定时器（VCPLog/Info 冷断连）和 5 分钟 linger 定时器（分布式保活冷断连）
- `set_app_foreground_state(true)`：恢复 `APP_IN_FOREGROUND = true` → 取消所有 linger → 若已冷断连则自动重连

详见 [vcp_log_service（modules/21）](../modules/21_基础设施杂项.md) 和 [vcp_info_service（modules/23）](../modules/23_RAG灵视中心.md)。

---

## 4. 事件格式标准

```kotlin
private fun emit(eventName: String, detail: Map<String, Any?>) {
    val json = serializeValue(detail)
    val script = "window.dispatchEvent(new CustomEvent('$eventName', { detail: $json }))"
    webViewRef?.evaluateJavascript(script, null)
}
```

与 `KeyboardInsetsManager` 共用相同的 `serializeValue` / `escapeJson` 实现，保持序列化行为一致。

---

## 5. 前端接收方式

```typescript
window.addEventListener('vcp-lifecycle', (e: CustomEvent) => {
    const { state, isDarkMode } = e.detail;
    switch (state) {
        case 'resume':
            // 恢复连接
            break;
        case 'pause':
            // 暂停操作
            break;
        case 'stop':
            // 释放资源
            break;
        case 'config-changed':
            // 切换主题
            toggleTheme(isDarkMode);
            break;
        case 'low-memory':
            // 清理缓存
            clearImageCache();
            break;
    }
});
```

---

## 5. 生命周期绑定

```kotlin
// VcpMobilePlugin.kt
class VcpMobilePlugin(private val activity: Activity) : Plugin(activity) {
    private val lifecycleBridge = LifecycleBridge()

    override fun load(webView: WebView) {
        super.load(webView)
        lifecycleBridge.attach(activity, webView)
    }
}
```

### 5.1 `attach()` 流程（v1.1.2: ProcessLifecycleOwner）

```kotlin
fun attach(activity: Activity, webView: WebView) {
    webViewRef = webView
    activity.runOnUiThread {
        ProcessLifecycleOwner.get().lifecycle.addObserver(this)
    }
}
```

- **`ProcessLifecycleOwner.get()`**：返回进程级生命周期持有者（来自 `lifecycle-process` 库）。
- **`runOnUiThread`**：确保 UI 线程上注册观察者（Android 生命周期框架线程安全要求）。
- **`addObserver(this)`**：将 `LifecycleBridge`（即 `DefaultLifecycleObserver`）注册到进程生命周期。

### 5.2 观察者清理

```kotlin
override fun onDestroy(owner: LifecycleOwner) {
    super.onDestroy(owner)
    try {
        ProcessLifecycleOwner.get().lifecycle.removeObserver(this)
    } catch (e: Exception) {
        Log.w(TAG, "Failed to remove lifecycle observer", e)
    }
}
```

`onDestroy` 在进程生命周期结束时触发，清理观察者注册。`try-catch` 确保即使 Activity 已被回收也不会崩溃。

---

## 6. 与 `KeyboardInsetsManager` 的对比

| 维度 | `KeyboardInsetsManager` | `LifecycleBridge` |
|------|------------------------|-------------------|
| 监听目标 | `WindowInsets` 变化 | **进程**生命周期 |
| 注册方式 | `ViewCompat.setOnApplyWindowInsetsListener` | `ProcessLifecycleOwner.get().lifecycle.addObserver` |
| 事件名 | `vcp-keyboard-inset` | `vcp-lifecycle` |
| 触发频率 | 高（键盘动画） | 极低（前台/后台切换） |
| v1.1.2 变化 | 无 | 升级为进程级观察 + 桥接 Rust `set_app_foreground_state` |

---

## 7. 关键约束

1. **`Plugin.load(webView)` 时机**：`attach()` 必须在 `load()` 中调用，确保 WebView 已初始化。参见 `docs/ANDROID_PLUGIN_MANAGEMENT.md` §7.3。

2. **`lifecycle-process` 依赖必需**：移除 `lifecycle-process:2.7.0` 会导致 `ProcessLifecycleOwner` 不可用，生命周期事件将回退为 Activity 级别（可能丢失前后台消抖优势）。

3. **`onDestroy` 清理是防御性的**：进程级观察者在进程存活期间一般不需要手动移除，但为防内存泄漏，`onDestroy` 中仍做 `removeObserver` 清理，外包 `try-catch` 避免 Activity 已回收时崩溃。

4. **与 Rust 后台 linger 耦合**：生命周期的 pause/resume 直接触发 `set_app_foreground_state` Tauri 命令，进而影响 VCPLog/Info 连接状态和分布式保活模式。修改生命周期事件时需同步验证 Rust 侧的 linger 行为。

## 8. 交叉参考

| 文档 | 关系 |
|------|------|
| `docs/modules/21_基础设施杂项.md` | `set_app_foreground_state` 命令的 Rust 实现 + linger 定时器 |
| `docs/modules/23_RAG灵视中心.md` | `vcp_info_service` 的前台感知事件丢弃 |
| [11_ForegroundGuardian](11_ForegroundGuardian_前台守护者.md) | 后台 linger 到期后的分布式保活释放 |
| `docs/ANDROID_PLUGIN_MANAGEMENT.md` | 进程级生命周期开发规范 |
